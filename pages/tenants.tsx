import React from 'react'
import Header from '../Components/Header';
import Navbar from '../Components/Navbar';

interface tenantsProps {
  
}

const tenants = ({}: tenantsProps) => {
  return (
    <div>
      <Header id={2} />
      <Navbar />
      tenants
    </div>
  );
}

export default tenants
