import React from "react";
import Header from "../Components/Header";
import Navbar from "../Components/Navbar";

interface landlordsProps {}

const landlords = ({}: landlordsProps) => {
  return (
    <div>
      <Header id={1} />
      <Navbar />
      landlords
    </div>
  );
};

export default landlords;
